name: release

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  trigger_wheels:
    uses: ./.github/workflows/wheels.yml
    permissions:
      contents: read
      id-token: write
