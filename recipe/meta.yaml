{% set name = "merton" %}
{% set version = "1.0.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 68a25e463eac1340de77ef351267748c72b15530ce92e38a3d69b80d181a6a1a

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  entry_points:
    - merton = merton.cli.main:app

requirements:
  host:
    - python >=3.11
    - pip
    - hatchling >=1.27
    - hatch-vcs >=0.4
  run:
    - python >=3.11
    - numpy >=1.26
    - scipy >=1.13
    - pandas >=2.2
    - pyarrow >=15
    - numba >=0.61
    - array-api-compat >=1.9
    - structlog >=24.4
    - pydantic >=2.8
    - pydantic-settings >=2.5
    - typing_extensions >=4.12
    - platformdirs >=4.3
    - joblib >=1.4
    - typer >=0.12
    - rich >=13.7
    - tomli-w >=1.0

test:
  imports:
    - merton
    - merton.calibration
    - merton.extensions
    - merton.portfolio
    - merton.backtest
    - merton.scenarios
    - merton.scenarios.predefined.ngfs
  commands:
    - pip check
    - merton --help
    - merton doctor

about:
  home: https://github.com/ItsSypher/merton
  summary: Production-grade Merton structural credit-risk model and extensions.
  description: |
    `merton` is a vectorized, GPU-capable Python package for the Merton
    (1974) structural credit-risk model and its industry-standard
    descendants — KMV, Black-Cox first-passage, Geske compound options,
    Longstaff-Schwartz stochastic rates, CreditGrades, Leland-Toft
    endogenous default, Zhou jump-diffusion, and NGFS-aligned climate
    overlays. It bundles a portfolio engine (Vasicek single-factor with
    Basel-IRB closed forms, Gaussian/t copulas, Monte Carlo loss
    distribution), a backtesting harness (AUC, Brier, KS, calibration
    plots), and Excel integration via xlwings Server.
  license: Apache-2.0
  license_family: APACHE
  license_file: LICENSE
  doc_url: https://merton.readthedocs.io
  dev_url: https://github.com/ItsSypher/merton

extra:
  recipe-maintainers:
    - ItsSypher
